# CPU-first install helper for Linux/Docker environments.
#
# Use this before installing requirements.txt when you want to avoid NVIDIA
# CUDA wheels on machines that do not have NVIDIA GPUs.
torch>=2.2,<3
torchvision>=0.17,<1

python-docx>=1.1.0
python-pptx>=1.0.2
beautifulsoup4>=4.12.0
html2text>=2024.2.26
openpyxl>=3.1.2
